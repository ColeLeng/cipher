import type { EntityMap } from "./orchestrator";

export function decipherResponse(_response: string, _entityMap: EntityMap): string {
  throw new Error("decipherResponse is not implemented yet.");
}
